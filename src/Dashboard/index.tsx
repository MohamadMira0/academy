import LayoutDashboard from './LayoutDashboard';

interface IProps {}
const Dashboard = ({}: IProps) => {
  return (
    <div dir="rtl">
      <LayoutDashboard />
    </div>
  );
};

export default Dashboard;
